@default = "robotType"
@summary = "Type to the focused element the value '${value1}'"
definition {

	function robotType(value1) {
		WaitForSPARefresh();

		selenium.robotType(${value1});
	}

	function robotTypeShortcut(value1) {
		WaitForSPARefresh();

		selenium.robotTypeShortcut(${value1});
	}

}