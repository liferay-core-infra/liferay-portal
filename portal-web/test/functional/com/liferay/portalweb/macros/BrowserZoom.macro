definition {

	@summary = "Default summary"
	macro resetZoom() {
		RobotType.robotTypeShortcut(value1 = "Ctrl + 0");
	}

	@summary = "Default summary"
	macro setZoomTo200() {
		BrowserZoom.resetZoom();

		RobotType.robotTypeShortcut(value1 = "Ctrl + + + + + + + + + +");
	}

}