ext {
	gitHash = "6d952d18ac7b728684be5724df7a3bc8bc427d12"
	gitHubOwner = "liferay"
	gitHubRepository = "liferay-faces-alloy"
	mvnJARInclude = "com.liferay.faces.alloy-5.0.0.jar"
	mvnTargetDir = "alloy/target"
}

apply from: "../build-maven.gradle"

downloadSource {
	doLast {
		File pomXmlFile = new File(mvnSourceDir, "pom.xml")

		if (pomXmlFile.exists()) {
			pomXmlFile.text = pomXmlFile.text.replace("<module>demo</module>", "")
		}
	}
}